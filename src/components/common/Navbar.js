import React from 'react';

const SideNav = (props) => {

    return (
        <div className = "sideNav">
            <a href="#soloSession">Solo ACT Matrix</a>
            <a href="#teamSession">Team Session</a>
            <a href="#settings">Settings</a>
        </div>
    );


};

export default SideNav;